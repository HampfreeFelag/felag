# 🎨 Редизайн «Félag Editorial Minimal» — 15.09.2026

## Статус: ✅ Готов локально, НЕ выкатан (maintenance ON)

Редизайн завершён и проверен на dev-сервере. Прод (felag.online) остаётся в maintenance-режиме
до ревью и решения о выкате. **Чтобы посмотреть: `npm run dev` → http://localhost:4321/**

---

## Что сделано

### Дизайн-система (полный перепис `src/styles/global.css`)
- Карточки → **hairline-списки**: посты — типографические строки с тонкими разделителями
  (референсы: satnaing.dev, lekoarts.de, astro-micro)
- **Кикеры/рубрики**: моно-шрифт, uppercase, янтарный акцент `#A97B2C`
- **Палитра**: бумага `#FAF9F6`, фьорд-синий `#1E3A5F` (ссылки), янтарь (акценты)
- Плоские радиусы (3–4px), убраны тени и подъёмы карточек
- Поиск — одна нижняя линия; категории-фильтры — pill-кнопки
- Отступы: `--page-padding: clamp(20px, 5vw, 48px)`, сетка 1120px

### Страницы
- **Главная**: hero с kicker-строкой, текстовая CTA (`.text-link` со стрелкой),
  рунный орнамент `ᚠᛖᛚᚨᚷ · ᚱᛖᛗᛖᛊᛚᛟ` (убран SVG-фон NovelScene)
- **Логотип**: wordmark `FÉLAGᛉ` (вместо длинного SITE_TITLE в шапке)
- **Пост** (`BlogPost.astro`): мета-строка → крупный serif-заголовок → лид; чистый TOC;
  related-посты списком; disclaimer — янтарная левая линия
- **About**: убраны ~90 inline-стилей, теперь `.prose`
- **Library/категории**: плоский список, `.page-title`

### Попутно исправленные баги
1. **EN-категории не генерировались** — getStaticPaths в `[category].astro` был только для `ru`
2. `layout-grid` / `nes-container` использовались, но не определены в CSS → пост-страница была сломана
3. Disclaimer ссылался на неподключённый шрифт 'Press Start 2P'
4. Мёртвые CSS-переменные (`--space-1/2/3/5/6`, `--color-text-primary`, `--nordic-*`) почищены

## Проверка (15.09.2026)
- 12 URL → HTTP 200 (RU/EN: главная, library, about, privacy, посты, категории)
- Puppeteer: 8 скриншотов, 0px переполнения на 1440px и 390px (скриншоты: `.screens/`, gitignored)
- `npx astro build`: 38 страниц, ~5 сек, без ошибок

---

## ⏭️ Следующие шаги (продолжение 16.09)

1. **Ревью на dev**: `npm run dev`, пройтись по всем страницам глазами, полировки
2. **Выкат**: в `src/middleware.ts` поставить `MAINTENANCE_MODE = false`, закоммитить, запушить
   (deploy.yml автоматически соберёт и задеплоит на felag.online)
3. **Не решено (осознанно)**:
   - ~50 RU-статей в `draft: true` — публикация на усмотрение владельца
   - Варнинги glob-loader о дубликатах id в `src/content/blog-ru` (пре-существующие)
   - Мёртвые компоненты не удалялись: `FAQ.astro`, `NordicScene.astro`, `SubscribeForm.astro`,
     `TerminalLayout.astro` и др. (не используются, но не мешают — кандидаты на чистку)
   - `scripts/` содержат ссылки на старые пути (gitverse-tradfi) — не влияют на сборку

## Ключевые файлы редизайна
| Файл | Что там |
|---|---|
| `src/styles/global.css` | вся дизайн-система (токены, хедер, списки, prose, TOC) |
| `src/components/Banner.astro` | элемент списка постов (рубрика → заголовок → описание) |
| `src/layouts/BlogPost.astro` | лейаут поста: layout-grid + TOC + related + disclaimer |
| `src/pages/[lang]/index.astro` | hero + последние посты |
| `src/middleware.ts` | выключатель maintenance |
